## If you use Spring Cloud Zookeeper Config,
## the values shown in the preceding example need to be in bootstrap.yml instead of application.yml
spring:
  cloud:
    zookeeper:
      connect-string: 192.168.0.111:2181